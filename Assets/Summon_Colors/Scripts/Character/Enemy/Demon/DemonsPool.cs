using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DemonsPool : MonoBehaviour
{
    [SerializeField] private ObjectPoolBase _breathPool;
    [SerializeField] private ObjectPoolBase _meadiumFirePool;
    [SerializeField] private ObjectPoolBase _bigFirebreathPool;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
